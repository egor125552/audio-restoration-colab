name: Manual stem preview patch check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-patch-script:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Verify helper syntax
        run: python -m py_compile scripts/apply_stem_preview_fixes.py
